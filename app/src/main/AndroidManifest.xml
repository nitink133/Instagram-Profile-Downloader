<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.InstaDownload.stories.profile.post.download">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".AppController"
        android:allowBackup="true"
        android:icon="@mipmap/instadownloader_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/instadownloader_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.InterstitialAdActivity"></activity>
        <activity android:name=".activity.DownloadFromUrlActivity" />
        <activity android:name=".activity.SearchActivity" />
        <activity android:name=".activity.InstagramOfficalLoginActivity" />
        <activity android:name=".activity.HowToUseActivity" />
        <activity android:name=".activity.DownloadProfileImageActivity" />
        <activity android:name=".activity.ViewStoryActivity" />
        <activity android:name=".activity.ViewProfileActivity" />
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".activity.DownloadHistoryActivity" />
        <activity
            android:name=".activity.ProfilepPictureActivity"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity android:name=".activity.StoriesDownloadActivity" />
        <activity
            android:name=".activity.splash.SplashActivity"
            android:theme="@style/Theme.Design.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.introscreen.IntroScreenActivity"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity
            android:name=".activity.dashboard.MainActivity"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity android:name=".helper.report.ErrorActivity" />

        <service
            android:name=".firebase.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <!--
 Set custom default instadownloader_icon. This is used when no instadownloader_icon is set for incoming notification messages.
     See README(https://goo.gl/l4GJaQ) for more.
        -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher" />
        <!--
 Set color used with incoming notification messages. This is used when no color is set for the incoming
             notification message. See README(https://goo.gl/6BKBk7) for more.
        -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/colorAccent" />
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-7814549536543810~2265774028" />
    </application>

</manifest>